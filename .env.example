# =============================================
# Login App（ユーザー認証 + Token Exchange 共用）
# =============================================
OKTA_DOMAIN=https://your-org.okta.com
OKTA_CLIENT_ID=your-login-app-client-id
OKTA_CLIENT_SECRET=your-login-app-client-secret

# =============================================
# Token Exchange 設定
# =============================================
# audience: Token Exchange のデフォルト audience（任意）
# 未設定の場合は Token Exchange リクエスト時にリクエストボディで audience を指定する。
# サードパーティアプリに渡す場合は呼び出し元が audience を指定するため、通常は不要。
# テスト用にデフォルト値を設定したい場合のみ記述する。
# 例: # OKTA_RESOURCE_AUDIENCE=https://your-org.okta.com/oauth2/default

# =============================================
# NextAuth
# =============================================
AUTH_SECRET="secret" # npx auth secret で生成する
