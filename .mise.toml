# mise設定ファイル
# Nix環境は .envrc と shell.nix で管理されます

[env]
# 環境変数の設定（必要に応じて）
# AWS_REGION = "ap-northeast-1"

[tasks.setup]
description = "開発環境のセットアップ"
run = """
echo "Nix環境が有効になっています"
echo "AWS SSOを設定する場合は: aws configure sso"
"""

[tasks.tf-init]
description = "Terraformの初期化"
run = "cd terraform && terraform init"

[tasks.tf-plan]
description = "Terraformプランの実行"
run = "cd terraform && terraform plan"

[tasks.tf-apply]
description = "Terraformの適用"
run = "cd terraform && terraform apply"

[tasks.sso-login]
description = "AWS SSOログイン"
run = "aws sso login --profile ${AWS_PROFILE:-default}"
