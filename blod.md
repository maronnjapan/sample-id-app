## 言いたいこと
OktaでCIBAが使えない時、特定の操作時に承認を挟みたいなら[User Factor API](https://developer.okta.com/docs/api/openapi/okta-management/management/tag/UserFactor/#tag/UserFactor/operation/verifyFactor)が良いと思う。
## 伝えたい人
CIBAとOktaのUser Factor APIによる認証は理解しているが、OktaでCIBAを使えるか使えない場合どうすればよいか決めあぐねている人

## 目次

- はじめに
  - 高リスク操作の時にユーザーに承認を求めるようにしたい
  - 出てくる選択肢はCIBAがある。
  - ただ、OktaでCIBAはハードルが高い
  - なので、承認という点でいうならUser Factor APIによるプッシュ通知ができるかも
  - もちろん課題はあるが、一考の余地はあると思う。  
- OktaでCIBAをやるには
  - カスタム認可サーバーの用意
    - ライセンスが別途必要
  - もしくは、Org 認可サーバーを用意
  - Org 認可サーバーの場合、カスタムAuthenticatorが必要
  - プッシュ通知機能が搭載されたアプリを用意する必要がある
  - かなり実装のハードルが高い
  - CIBAを行うにはハードルが高い 
 - OktaでCIBAの代わりを考える
  - 特定の操作前にユーザーの承認を得て操作を続行するというケースだけに限定するなら、以下の条件を満たせばよい
    - ユーザーによる承認操作
    - 通知が特定の操作のものであることの担保
  - 以下の二つのパターンがあると考える
    - User Factor APIを使い端末に通知を送り、通知を許可したら操作を実行する
    - OIDCやSAMLによる認証を再度行い、認証できたことを示せれば操作を実行する
- User Factor APIを使った通知による承認
  - User Factor APIを使って承認を求めるタイミングでプッシュ通知を送る
  - ポーリングしてプッシュ通知の操作が完了すれば操作を実行する
  - プッシュ通知を再認証用であることを担保するために、NumberChallengeを設定する。
    - サービス側の画面で出ている数字をプッシュ通知の画面で押すことで、想定外の通知を許可することを避ける
    - とはいえ、CIBAにおけるbinding_messageと違いカスタムメッセージは表示できないので、通知が何の通知なのかは認識する必要がある。
  - 利点：リダイレクトを必要としないので任意のユーザーに通知ができる。所有しているデバイスでの操作が前提となっており、CIBAの思想に近い。
  - 欠点：Okta独自APIを使っているので、セキュリティの担保が実装者にゆだねられる。binding_messageが使えないのでなんの承認かがわからない。
- OIDCやSAMLによる再認証
  - 承認を求める際に再度Oktaのログイン画面にリダイレクトさせる
  - 認証を行いIDトークンやSAMLアサーションを受け取り、検証が完了したら認証ができた証明できるので、承認を得たとして処理を実行する。
  - 利点：標準仕様にしたがっているので、適切に認証したことの担保がしやすい。
  - 欠点：リダイレクトが必要なので、操作している人しか再認証ができない。再認証用のコールバック処理を用意しておく必要があり、通常のコールバックとの切り分けが必要。
