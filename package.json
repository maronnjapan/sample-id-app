{
  "name": "oauth-cache-control-demo",
  "version": "1.0.0",
  "description": "OAuth Token Endpoint Cache-Control検証アプリ",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm run build && wrangler deploy",
    "build": "tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}