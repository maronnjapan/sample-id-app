# Mutations

The table-driven tests in `verifier_test.go` implement T01 through T22 and T24 directly against the anonymized fixtures.

This directory is kept as the place to add YAML-based mutation definitions when a real policy response introduces a new class of field or normalization rule.
